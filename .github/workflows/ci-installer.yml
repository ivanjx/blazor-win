name: Build sideload installer

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-installer:
    runs-on: windows-latest

    env:
      SolutionPath: BlazorHybrid.slnx
      PackagingProjectPath: src\BlazorHybrid.Packaging\BlazorHybrid.Packaging.wapproj
      AppPackagesDirectory: AppPackages

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: src/BlazorHybrid.ClientScripts/package-lock.json

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Install npm dependencies
        working-directory: src/BlazorHybrid.ClientScripts
        run: npm ci

      - name: Create signing certificate
        shell: pwsh
        run: |
          $cert = New-SelfSignedCertificate -Type Custom -Subject "CN=BlazorHybrid CI" -KeyUsage DigitalSignature -FriendlyName "BlazorHybrid CI" -CertStoreLocation Cert:\CurrentUser\My -NotAfter (Get-Date).AddYears(5)
          $emptyPassword = ConvertTo-SecureString "" -AsPlainText -Force
          $artifactRoot = Join-Path $env:GITHUB_WORKSPACE $env:AppPackagesDirectory
          New-Item -ItemType Directory -Force -Path $artifactRoot | Out-Null
          $pfxPath = Join-Path $artifactRoot "BlazorHybrid.CI.pfx"
          $cerPath = Join-Path $artifactRoot "BlazorHybrid.CI.cer"
          Export-PfxCertificate -Cert $cert -FilePath $pfxPath -Password $emptyPassword | Out-Null
          Export-Certificate -Cert $cert -FilePath $cerPath | Out-Null
          "SIGNING_CERT=$pfxPath" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Restore solution
        run: dotnet restore $env:SolutionPath -p:RuntimeIdentifier=win-x64

      - name: Build solution
        run: dotnet build $env:SolutionPath -c Release --no-restore -p:RuntimeIdentifier=win-x64

      - name: Build sideload package
        run: >
          msbuild $env:PackagingProjectPath /t:Build
          /p:Configuration=Release
          /p:Platform=x64
          /p:RuntimeIdentifier=win-x64
          /p:UapAppxPackageBuildMode=SideloadOnly
          /p:AppxBundle=Never
          /p:AppxPackageDir="$env:GITHUB_WORKSPACE\AppPackages\"
          /p:AppxPackageSigningEnabled=true
          /p:PackageCertificateKeyFile="$env:SIGNING_CERT"
          /p:PackageCertificatePassword=""

      - name: Upload installer artifacts
        uses: actions/upload-artifact@v4
        with:
          name: blazorhybrid-sideload-installer
          path: |
            AppPackages\
